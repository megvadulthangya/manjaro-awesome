post_install() {
    # Inicializáld a pacman kulcstartót, ha még nincs
    if ! pacman-key -l >/dev/null 2>&1; then
        pacman-key --init
    fi
    # Töltsd be a kulcsot és a bizalmi beállításokat
    pacman-key --populate manjaro-awesome
}

post_upgrade() {
    post_install
}
